apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-rs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
      type: frontend
  template:
    metadata:
      labels:
        app: nginx
        type: frontend
    spec:
      containers:
      - name: my-app
        image: nginx
