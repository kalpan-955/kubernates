apiVersion: v1
kind: Service
metadata:
  name:  name-service
spec:
  type: ClusterIP
  selector:
    app: nginx
    type: frontend
  ports:
    - name: http-request
      port: 8080
      targetPort: 80
 
